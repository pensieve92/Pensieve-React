import React from 'react';
import styles from './BoardBody.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const BoardBody = () => (
  <div className={cx('BoardBody')} >
    BoardBody
  </div>
);

export default BoardBody;
