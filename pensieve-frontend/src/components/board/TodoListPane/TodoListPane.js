import React from 'react';
import styles from './TodoListPane.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const TodoListPane = () => (
  <div className={cx('TodoListPane')} >
    TodoListPane
  </div>
);

export default TodoListPane;
